# Copia este archivo a `.env` y ajusta los valores para cada entorno (dev/staging/produccion)
# Nunca subas `.env` al repositorio.

# App
SPRING_APPLICATION_NAME=ms-inventario
SERVER_PORT=8081

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=toolrent_db
DB_USER=root
DB_PASSWORD=change_me

# JPA / Hibernate
JPA_DDL_AUTO=none
HIBERNATE_PHYSICAL_STRATEGY=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Seguridad (Resource Server JWT)
SPRING_SECURITY_JWT_ISSUER_URI=http://localhost:9000/realms/toolrent
# Si usas audiencias, puedes habilitarlo en código o properties adicionales
# SPRING_SECURITY_JWT_AUDIENCES=account

# Eureka (Service Discovery)
EUREKA_URL=http://localhost:8761/eureka/

